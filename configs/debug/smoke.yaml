# @package _global_
debug:
  enabled: true

training:
  max_steps: 2
  gradient_accumulation_steps: 1
  eval_every_steps: 1
  save_every_steps: 1
  log_every_steps: 1

evaluation:
  generation_subset_size: 2
  max_new_tokens: 128

data:
  train_max_samples: 4
  val_max_samples: 2
  num_workers: 0

logger:
  wandb:
    enabled: false

