Using the current semantic hypothesis below and the input drawing, produce an ordered plan of modelling operations that constructs the part. The plan must name the operation each step performs, the features it produces, and where on the part it lands. Measurements the hypothesis already holds are cited rather than copied; see below.

Semantic hypothesis:

$semantic_hypothesis


$guidelines
