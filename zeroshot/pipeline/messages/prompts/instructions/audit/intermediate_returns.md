- `$returns_dir/<ret_name>/` — the same three artifacts for the solid each planned operation left behind, under the `ret_...` name that operation produces

An entry is written for every `ret_...` the program assigned, whether or not `result` itself built. Open the ones bearing on a defect you have found and see whether that operation built what the plan meant it to; that is what says which `ret_...` a backtrace should name.
